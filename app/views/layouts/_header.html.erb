<header class="navbar navbar-inverse navbar-fixed-top bs-docs-nav" role="banner">
	<div class="margin">
  	<div class="left">
			<button class="navbar-toggle" type="button" data-toggle="collapse"
							data-target=".bs-navbar-collapse">
      	<span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
			<%= link_to root_path, class: "navbar-brand", id: "logo" do %>
				<span style=:"color:#000000; letter-spacing:-2px; font:26px/28px Arial, Helvetica, sans-serif;">Sensor</span><span style="color:#e64c4c; letter-spacing:-1px; font:bold 26px/28px Arial, Helvetica, sans-serif; margin-left: 5px;">Cloud</span>
			<% end %>
    </div>

    <nav class="collapse navbar-collapse bs-navbar-collapse" role="navigation">
    	<ul class="nav navbar-nav search-bar-header">
      	<li>

				<% unless (params[:controller] == 'searches'||
									 ((params[:controller] == 'static_pages') && (params[:action] == 'home'))) %>
						<%= form_for(:search, url: searches_path, class: "navbar-from navbar-left") do |f| %>
							<%= f.text_field :query, class: "form-control",
							  	placeholder: "Search..." %>
				</li>
				<li>
							<%= f.submit "Submit", class: "btn btn-info" %>
						<% end %>
					<% end %>

				</li>
     	</ul>

    	<ul class="nav navbar-nav">
				<% if signed_in? %>
        	<li><%= link_to "My Streams", streams_path %></li>
				<% end %>
			</ul>

			<ul class="nav navbar-nav right">
				<% if signed_in? %>
					<li>
						<%= link_to user_path(current_user), id: "name" do %>
							<%= gravatar_for current_user %>
							<%= current_user.name %>
						<% end %>
					</li>
				<% end %>
				<!--
				<li>
					<%= link_to '#' do %>
						<%= image_tag("anonymous.jpg", alt: "User's Avatar", :height => '32', :width => '32') %>
						Anonymous
					<% end %>
				</li>
				-->
				<li><%#= link_to "Home",    root_path %></li>
				<li><%#= link_to "Help", help_path %></li>
				<% if signed_in? %>
					<li><%= link_to "Users", users_path %></li>
					<li id="fat-menu" class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">
							Account <b class="caret"></b>
						</a>
						<ul class="dropdown-menu">
							<li><%= link_to "Profile", current_user %></li>
							<li><%= link_to "Settings", edit_user_path(current_user) %></li>
							<li class="divider"></li>
							<li>
								<%= link_to "Sign out", signout_path, method: "delete" %>
							</li>
						</ul>
					</li>
				<% else %>
					<li><%= link_to "Sign in", signin_path %></li>
          <li><%= link_to "Sign up", signup_path %></li>
				<% end %>
      </ul>
    </nav>
  </div>
</header>
