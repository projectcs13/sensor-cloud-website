<%= form_for(current_user.relationships.build(followed_id: @stream_id), remote: true) do |f| %>
	<div><%= f.hidden_field :followed_id %></div>
	<%= f.submit "Follow", class: "btn btn-lg btn-primary" %>
<% end %>
