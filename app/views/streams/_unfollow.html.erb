<%= form_for(current_user, :url => url_for(:controller => 'relationships', :action => 'destroy'), 
						 html: { method: :delete }, remote: true) do |f| %>
	<%= f.submit "Unfollow", class: "btn btn-lg" %>
<% end %>
