<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @stream.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @stream.description %>
</p>

<p>
  <strong>Private:</strong>
  <%= @stream.private %>
</p>

<p>
  <strong>Accuracy:</strong>
  <%= @stream.accuracy %>
</p>

<p>
  <strong>Longitude:</strong>
  <%= @stream.longitude %>
</p>

<p>
  <strong>Latitude:</strong>
  <%= @stream.latitude %>
</p>

<p>
  <strong>Type:</strong>
  <%= @stream.stream_type %>
</p>

<p>
  <strong>Unit:</strong>
  <%= @stream.unit %>
</p>

<p>
  <strong>Max val:</strong>
  <%= @stream.max_val %>
</p>

<p>
  <strong>Min val:</strong>
  <%= @stream.min_val %>
</p>

<p>
  <strong>Active:</strong>
  <%= @stream.active %>
</p>

<p>
  <strong>Tags:</strong>
  <%= @stream.tags %>
</p>

<p>
  <strong>Resource:</strong>
  <%= @stream.resource_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @stream.user_id %>
</p>

<p>
  <strong>User ranking:</strong>
  <%= @stream.user_ranking %>
</p>

<p>
  <strong>History size:</strong>
  <%= @stream.history_size %>
</p>

<p>
  <strong>Subscribers:</strong>
  <%= @stream.subscribers %>
</p>

<%= link_to 'Edit', edit_resource_stream_path(@resource, @stream) %> |
<%= link_to 'Back', resource_streams_path(@resource) %>


<script type="text/javascript"> 
    /* functionality for creating a chart and filling with
        dummy data.
        This is just a placeholder until real data is available
      */
        var parseDate = d3.time.format("%Y-%m-%d").parse;
        var data = [];
        var year = 2012;
        var testchart = timeChart()
                        .height(200)
                        .x(function(d) { return parseDate(d.date) })
                        .y(function(d) { return d.value});

        setInterval(function(){
                newvalue = Math.random() * 3000;
                year += 2;
                data.push({value:newvalue, date: year +"-12-12"});
                d3.select("#example1")
                .datum(data)
                .call(testchart);

        }, 2000);
</script>

<div id="example1"></div> <!-- this is the placeholde div for our chart -->