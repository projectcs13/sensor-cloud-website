<% unless current_user?(@stream_owner) %>
	<div id="follow_form">
		<% if current_user.following?(@stream.id, openid_metadata) %>
			<%= render 'unfollow' %>
		<% else %>
			<%= render 'follow' %>
		<% end %>
	</div>
<% end %>
